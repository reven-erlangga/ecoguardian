apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: postgres-notif
  namespace: ecoguard
spec:
  serviceName: postgres-notif
  replicas: 1
  selector:
    matchLabels:
      app: postgres-notif
  template:
    metadata:
      labels:
        app: postgres-notif
    spec:
      containers:
        - name: postgres
          image: postgres:16-alpine
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_DB
              value: ecoguard_notif
            - name: POSTGRES_USER
              value: ecoguard
            - name: POSTGRES_PASSWORD
              value: ecoguard123
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 5Gi
---
apiVersion: v1
kind: Service
metadata:
  name: postgres-notif
  namespace: ecoguard
spec:
  selector:
    app: postgres-notif
  ports:
    - port: 5432
      targetPort: 5432
