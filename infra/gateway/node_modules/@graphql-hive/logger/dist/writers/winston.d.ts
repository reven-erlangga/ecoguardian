import { Logger } from 'winston';
import { b as LogWriter, L as LogLevel, a as Attributes } from '../logger-tyqDUP6r.js';
import '@whatwg-node/disposablestack';

declare class WinstonLogWriter implements LogWriter {
    #private;
    constructor(winstonLogger: Logger);
    write(level: LogLevel, attrs: Attributes | null | undefined, msg: string | null | undefined): void;
}

export { WinstonLogWriter };
